[project]
name = "openai-codex-claude-code-proxy"
version = "0.1.0"
description = "Run Claude Code through Azure OpenAI or OpenAI Codex OAuth using CLIProxyAPI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "litellm[proxy]>=1.91.2",
]
